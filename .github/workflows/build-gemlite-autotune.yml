name: GemLite Autotune

on:
  push:
    branches:
      - main
    paths:
      - 'gemlite_autotune/**'
      - 'common_utils/**'
      - '.github/workflows/build-gemlite-autotune.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'gemlite_autotune/**'
      - 'common_utils/**'
      - '.github/workflows/build-gemlite-autotune.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:latest

      - name: cleanup space on github vm
        run: |
          rm -rf /usr/share/dotnet \
                /opt/ghc \
                "$AGENT_TOOLSDIRECTORY"
            
          docker system prune -af || true
          df -h
  
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: gemlite_autotune/Dockerfile
          push: false
          tags: gemlite-autotune:latest

      - name: Test Docker image
        run: |
          docker run --rm gemlite-autotune:latest uv --version
          docker run --rm gemlite-autotune:latest python3.12 --version

      - name: Clean up Docker resources
        if: always()
        run: |
          docker system prune -af --volumes
          df -h